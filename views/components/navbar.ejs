<nav class="navbar navbar-expand-lg bg-dark">
    <div class="container-xxl">
        <div class="me-auto">
            <a class="navbar-brand" href="/">KUSSSSO</a>
        </div>
        <div>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav">
                <% if (typeof currentUser == 'undefined') { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/login"><i class="bi bi-door-open me-2"></i>Login</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/signup"><i class="bi bi-person-fill-add me-2"></i>Sign up</a>
                    </li>
                <% } else { %>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false"><i class="bi bi-person-circle me-2"></i><%= currentUser.username %></a>
                        <ul class="dropdown-menu">
                            <li>
                                <a class="dropdown-item" href="#"><i class="bi bi-person me-2"></i>Profile</a>
                            </li>
                            <% if (!currentUser.is_member) { %>
                                <li>
                                    <a class="dropdown-item" href="/membership"><i class="bi bi-stars me-2"></i>Be a member</a>
                                </li>
                            <% } %>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <a class="dropdown-item" href="/logout"><i class="bi bi-box-arrow-right me-2"></i>Log out</a>
                            </li>
                        </ul>
                    </li>
                <% } %>
            </ul>
        </div>
        </div>
    </div>
</nav>